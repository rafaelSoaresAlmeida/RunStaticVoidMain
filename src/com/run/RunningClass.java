package com.run;

import com.methods.ArraySort;
import com.methods.AscendingSliceMaximumSize;
import com.methods.CircularQueue;
import com.methods.MaxSubstringWithoutRepeatedCharacters;
import com.methods.deckOfCards.Deck;

import java.util.Queue;

public class RunningClass {

    static int n = 3;
    int m[][] = new int[n][n];

    static int matriz[][] = {{1, 2, 3}, {4, 5, 6}, {5, 8, 9}};

    static Integer[] integers = {9, 6, 8, 6, 0};
    static int[] ints = {9, 6, 8, 6, 0};

    public static void main(String[] args) {

       /* Integer[] array = {2, 2, 2, 2, 1, 2, -1, 2, 1, 3};
        Integer[] array2 = {30, 20, 10};
        System.out.println(AscendingSliceMaximumSize.allSlices(array2));
        System.out.println(AscendingSliceMaximumSize.slice(array2));
*/

        //TestList.somaDiagonal(matriz, 0, n);

        //  Matrix matrix = new Matrix(matriz, n);
        //  System.out.println(matrix.diffDiagonal());

  /*      ArraySort.sortArrayWithDuplicates(integers);
        ArraySort.sortArrayWithDuplicates(ints);
        ArraySort.sortArrayWithOutDuplicates(integers);
        ArraySort.reverse(integers);*/

/*
        CircularQueue circularQueue = new CircularQueue(5);
        circularQueue.enqueue("A");
        circularQueue.enqueue("B");
        circularQueue.enqueue("C");
        circularQueue.enqueue("D");
        System.out.println(circularQueue.size());
        System.out.println(circularQueue.dequeue());
        circularQueue.enqueue("E");
        System.out.println(circularQueue.size());
        System.out.println(circularQueue.dequeue());
        System.out.println(circularQueue.size());
*/


    //    Deck.deck();


   //     System.out.println(MaxSubstringWithoutRepeatedCharacters.lengthOfLongestSubstring("abcabcbb"));







    }
}
